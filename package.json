{
  "name": "food_delivery",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "gulnoor",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:backend": "rimraf dist/backend && tsc",
    "build:ui": "rimraf dist/public && cd ../frontend && npm run build && shx cp -r dist ../backend/dist/public",
    "build:full": "npm run build:backend && npm run build:ui",
    "start": "node dist/backend/index.js",
    "deploy:full": "npm run build:full && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/express": "^5.0.6",
    "@typescript/native": "npm:typescript@^7.0.2",
    "concurrently": "^10.0.5",
    "eslint": "^10.8.1",
    "globals": "^17.11.0",
    "rimraf": "^6.1.3",
    "shx": "^0.4.0",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.67.0"
  }
}
